extends:
  - airbnb-typescript/base
overrides:
  - files: ['**/*.ts', '**/*.js']
    parserOptions:
      project: 'tsconfig.json'
  - files: ['**/index.ts']
    rules:
      'import/prefer-default-export':
        - 0
rules:
  '@typescript-eslint/semi':
    - error
    - never
